# TODO

Suikoを実行して見つかった問題と、診断品質を上げるための評価課題を記録する。

機能数を揃えること自体は目的にしない。Suikoの単一バイナリ、オフライン実行、決定的な検出という境界を保ちながら、実際の診断品質またはAgent Skillの利用品質が下がる差だけを扱う。

## 比較スナップショット

2026-08-18に、Natural Japaneseの`0f1cc1c5a4e2aa7590598c88a15c213a60d9545a`と、書籍・連載29原稿を`essay`として比較した。Suikoはプロジェクト設定を無効化し、両者で読解負荷も有効にした。

| 観点 | Natural Japanese | Suiko | 一致 |
|---|---:|---:|---:|
| 通常finding | 1,001件 | 974件 | カテゴリ別プロファイル 1/29原稿 |
| 読解負荷 | 129件 | 137件 | カテゴリ別プロファイル 14/29原稿 |
| `outline`の抽出項目 | - | - | 29/29原稿 |
| `outline`の見出し統計 | - | - | 8/29原稿 |
| `terms`の候補数 | 1,049件 | 1,031件 | 候補集合 6/29原稿 |

findingや候補の件数が多いことを、精度が高いこととはみなさない。差分は正解ラベル付き評価集合で誤検知と見落としに分ける。

## 競合比較から採用した判断

2026-08-18時点の一次資料と実行結果から、競合の機能を次のように分類した。機能名だけを移植せず、Suikoの診断品質または利用経路を改善するものだけを未解決TODOへ接続する。

| 比較対象 | Suikoより強い能力 | Suikoでの扱い | 対応TODO |
|---|---|---|---|
| Natural Japanese | SudachiPy基準の形態素検出、広い用語候補、任意の意味的検出 | 実コーパスで差を測り、Linderaで精度が上がる差は残す | 検出器の再校正、`terms`精度、意味的検出 |
| textlint | 列・範囲、formatter、修正候補、MCP、processorとルール資産 | 位置情報と安全な提案を先に採用し、汎用プラグインとMCPは利用価値を測ってから判断する | finding位置、安全なdiff、連携経路、拡張性 |
| AI writing preset | 短い文書でも発火する局所的なAI記述パターン | 文書全体の密度検出とは別カテゴリで評価する | 局所AIパターン |
| prh | 宣言的な表記辞書、差分表示、明示的な置換 | 一般的な置換器は再実装せず、複数文書の用語監査と安全な提案に限定して取り込む | `terms`精度、安全なdiff |
| RedPen | 複数マークアップ、注釈による抑制、拡張、エディタ連携 | 実利用がある形式と連携だけを候補にする | 連携経路、対応形式 |
| Vale | 単一バイナリ、YAMLスタイル、vocabulary、複数形式、エディタ/CI連携 | 配布形態は参考にし、任意ルール実行より宣言的な語彙を優先する | `terms`精度、連携経路、拡張性 |

Suikoが維持する差別化は、埋め込み辞書を含む単一バイナリ、実行時ダウンロードなし、同じ入力に同じfindingを返すこと、日本語の自然さと構造へ対象を絞ることである。競合に存在するという理由だけで、自動修正、重量モデル、任意コード実行型プラグインを既定機能へ入れない。

### 追加調査した競合

次のサービスとツールは公開された公式仕様を比較した。アカウント、契約、原稿の外部送信が必要なものは実行していないため、検出精度の優劣は未評価である。

| 比較対象 | 確認できた能力 | Suikoへの示唆 |
|---|---|---|
| [Shodo](https://developer.shodo.ink/docs/getting-started/try-shodo-api) | 日本語AI校正API、Python CLI、同期/非同期クライアント、行・文字位置を持つ結果 | 位置情報とAgent連携の参考になる。クラウド認証をSuiko本体へ持ち込まず、比較する場合も合成fixtureだけを送る |
| [Typoless](https://typoless.asahi.com/feature) | AIと大規模ルール辞書、カスタム辞書、良文支援、D&I/炎上リスク、Office・PDF・API連携 | 一般校正、組織辞書、リスク表現、バイナリ文書連携はSuikoより広い。機密文書を外へ出さないSuikoの境界と分けて評価する |
| [Just Right!7 Pro](https://www.justsystems.com/jp/products/justright/) | 誤字・誤用、不適切表現、表記揺れ、用語基準、訂正候補、公用文ルール | 一般校正の網羅性は再実装せず、Suikoが自然さへ集中する判断を実コーパスで確認する |
| [文賢](https://help.bun-ken.net/manual/) | 校正と推敲の分離、推奨語、コメント、補足、管理メモを持つ共有辞書、漢字使用率 | `.suiko.toml`の許可理由を、将来の用語監査でも失わない。単純な置換対だけで辞書を設計しない |
| [enno](https://enno.jp/) | 誤字脱字とタイポへ対象を絞った安定したパターン検出、誤検出抑制 | 少数の高精度ルールを優先する設計の比較対象にする。一般タイポ検出を無制限に追加しない |
| [jReadability](https://jreadability.net/sys/q_and_a?lang=ja) | コーパス由来の式による6段階の日本語難易度、語彙情報、500〜1,000字程度を前提とした評価 | 自然度、読解負荷、対象読者にとっての難易度を同じスコアへ混ぜない。難易度を出すなら文書長と対象読者の適用範囲を示す |
| [LanguageTool](https://dev.languagetool.org/languages) | 多言語の文法・スタイルAPIと宣言的ルール。日本語にも公開ルールがある | 日本語自然さの直接比較より、ルールID、置換候補、HTTP/エディタ連携の参考にする |
| Harper | Rust製、オフライン、Wasm、LSP、エディタ統合を持つ文法チェッカー | 現在は英語専用なので検出精度の競合ではない。Rustライブラリ分離、低遅延、組み込み配布の設計比較に限る |

商用サービスの非公開辞書件数や宣伝上の精度を、Suikoの目標値には使わない。比較実験を行う場合は、利用規約で許可された合成fixtureを使い、入力データ、プラン、実行日、設定を記録する。

## P1: Lindera版の検出器を実コーパスで再校正できるようにする

### 観測結果

Suikoの二つの回帰fixtureでは、移植前後の検出結果が一致した。

| fixture | 通常 | `--experimental` |
|---|---:|---:|
| AI的な文書 | 両者25件 | 両者33件 |
| 自然な文書 | 両者0件 | 両者0件 |

しかし、この一致だけでは形態素解析器をLindera/IPADICへ替えた影響を評価できない。書籍・連載29原稿では、カテゴリ別プロファイルが一致したのは1原稿だけだった。主な差は次のとおりである。

| category | Natural Japanese | Suiko | 差 |
|---|---:|---:|---:|
| `repeated_sentence_lead` | 571 | 556 | -15 |
| `low_specificity` | 24 | 12 | -12 |
| `nominal_ending` | 3 | 0 | -3 |
| `low_burstiness` | 27 | 29 | +2 |
| `low_lexical_diversity_ttr` | 28 | 29 | +1 |
| `no_chain`（読解負荷） | 0 | 3 | +3 |

`nominal_ending`はNatural Japaneseが「名詞止めなし」と判定した3原稿で、Suikoが名詞止めをそれぞれ1件、4件、14件と数えたためfindingを落としている。`outline`の見出し数と抽出項目は全原稿で一致したが、形態素に依存する見出し統計は8原稿しか完全一致しない。

Suikoの検出数が少ないカテゴリも、直ちに検出漏れとは言えない。誤検知が減った可能性がある。`suiko-eval`には、ハッシュと出典を持つmanifest、発火ゼロを含む検出器別レポート、3カテゴリの閾値sweep、ジャンル別・文書長別分析、読解負荷の独立集計を追加した。

評価集合は人間文書2件、AI文書1件になった。人間文書には2015年6月の青空文庫テキスト版ランキングから、新字新仮名の論説「堕落論」を選び、ルビと注記だけを再現可能な手順で除いた本文を収録した。出典、底本、入力者、校正者、変更内容、利用条件、SHA-256を記録している。旧字旧仮名の企業文書は現代文の閾値へ混ぜず、文学作品は将来の文体耐性集合へ分ける。

「堕落論」では`repeated_sentence_lead`が20件、`low_lexical_diversity_ttr`が1件、形態素ベース翻訳調が4件発火した。多くは「私は」「人間は」の意図的反復や、当時から使われる「ことができる」であり、AI判定として扱えない。TTR閾値を0.45から0.40へ下げるとこの発火は消えるが、4,000字以上のAI文書がなく検出率を比較できないため、既定値は変更しなかった。文頭反復も閾値15で発火が消える一方、AI側が全閾値で0件なので採用根拠にならない。

見出し統計はLindera/IPADICの期待値を固定するfixtureを追加した。全体の名詞止め率0.75、主要品詞signature比0.5などを回帰テストで保持する。

### 期待する動作

- 移植前の件数一致ではなく、Suiko自身の誤検知率と検出率から閾値を決められる
- Linderaの分かち書きに依存するカテゴリを、解析器移植後の実測値で評価できる
- 検出器、閾値、ジャンル、文書長を変えたときの退行を、`suiko-eval`で再現可能に確認できる

### 設計上の確認事項

第三者のコーパス本文を無条件に複製しない。ライセンス、再配布可否、取得手順を分け、必要ならURLとハッシュだけを管理する。移植前後の完全一致も要求しない。形態素解析器の差が診断品質を改善するなら、その差は残す。

### 完了条件

- 出典、利用条件、SHA-256を持つ人間文書とAI生成文書を、各ジャンル・文書長へ増やす
- 文書単位の発火率だけでなく、正解ラベル付きサンプルからカテゴリ別の誤検知率と検出率を出す
- `repeated_sentence_lead`、`low_specificity`、名詞止め、語彙多様性、形態素ベース翻訳調、読解負荷をLinderaで再校正する
- sweep対象を3カテゴリから、実コーパスで差が出た`low_specificity`、名詞止め、読解負荷へ広げる
- 見出し統計の解析器差をfixtureへ落とし、意図した差は互換レポートに明記する
- 採用した閾値について、何を支え、何を支えないかをレポートへ残す
- 小さなfixtureテストと、重いコーパス評価を分離して運用する

## P1: `terms`を用語監査として信頼できる精度へ上げる

### 観測結果

英小文字、1文字の固有名詞判定、中黒で始まる候補を除外し、初出前後80文字で説明手掛かりを見るよう修正した。これにより、29原稿のSuiko固有候補は119件から57件、説明手掛かりの不一致は163件から2件へ減った。

一方、候補集合が一致するのは6原稿だけである。Natural Japaneseだけが拾う候補は75件、Suikoだけが拾う候補は57件残る。たとえばSuikoは`応急処置`、`境界線`、`現実世界`を落とす一方、文脈によって`毎日`や`Social`を候補にする。解析器の件数一致ではなく、専門用語として確認する価値があるかを評価する必要がある。

また、Suikoの`terms`はファイルごとの候補一覧に留まる。prhの置換辞書やValeのvocabularyのように、プロジェクト全体の表記揺れ、推奨語、禁止表記を監査する能力はない。

### 期待する動作

- 候補抽出の適合率と再現率を、正解ラベル付きfixtureで判断できる
- 複数ファイルで同じ概念の表記揺れと初出説明を一覧化できる
- 自動置換を前提にせず、プロジェクトの正本用語と例外理由を確認材料として出せる
- 推奨語だけでなく、適用範囲、説明、例外理由、管理上のメモを保持できる

### 完了条件

- カタカナ複合語、ASCII略語、製品名、日本語固有名詞、一般名詞ノイズを含む評価集合を追加する
- Natural Japaneseとの75件/57件の差をサンプリングして正解ラベルを付け、Lindera側の抽出条件を再校正する
- 複数ファイルの用語集計と表記揺れレポートについて、CLIとJSONの契約を先に決める
- 用語辞書を追加する場合は宣言的なデータに限定し、推奨語、理由、対象パス、例外を表現できる最小スキーマを決める
- CSVなどから取り込む場合は、正規化後の差分を表示してから保存し、未知フィールドや重複ルールを黙って捨てない
- 既定ではファイルを書き換えず、辞書の共有やimportも明示操作に限定する

## 解消済み: Agent Skillの発火範囲を評価する

### 観測結果

positive/negative 26件をtrain 18件、test 8件へ分けた。3名の独立評価者は変更前に全員17/18件を正しく分類し、過去文書からの`style-profile`作成だけをfalse negativeとした。descriptionへ本文に既存の「3〜5文書から具体的な執筆用プロファイルを作る」能力だけを追加した。

変更後は別の3名がtrain 18/18、sealed holdout 8/8を全員正しく分類した。英語、画像、Markdown整形、表記統一、論証・著者性だけのレビュー、ファイル操作、翻訳、要約、コードコメント操作は対象外のままである。結果、曖昧さ、再試行の有無、評価限界は`skills/suiko/evals/results.md`へ記録した。

## P2: 単発でも強いAI記述パターンを局所検出する

### 観測結果

textlintのAI writing presetは、太字とコロンを使う箇条書き、絵文字付き箇条書き、誇張表現、述語の直後にコロンとブロックを置く構造を局所的に検出する。Suikoは太字、箇条書き、絵文字を文書全体の密度で見るため、次の短い例を`--experimental --genre tech`で実行してもfindingは0件だった。

```markdown
革命的な技術で業界を変えます。
実行します:
- **重要**: これは重要な項目です
- ✅ 完了した項目です
```

密度検出は文書全体の均一さを見るために必要だが、短い文書や一箇所だけ強く目立つパターンを拾えない。

### 期待する動作

- 文書密度と、局所パターンのfindingを別カテゴリとして説明できる
- `使用方法:`のような自然な名詞ラベルと、`実行します:`のような述語+コロンを区別する
- `世界初`など事実として必要な表現を、根拠なく禁止語にしない

### 完了条件

- 箇条書き装飾、述語+コロン、誇張表現についてpositive/negative fixtureを先に作る
- 形態素情報で名詞ラベルと述語終止を区別し、Markdownの次ブロック種別も確認する
- 誇張表現は文脈なしの禁止ではなく`info`の確認候補とし、理由付き許可を利用できる
- 既存の密度カテゴリと同じ箇所を二重に報告するときの抑制方針を決める

## P2: 自然度・読解負荷・読者別難易度を分離する

### 観測結果

Suikoは通常findingと`reading_load`を分けているが、「この文章はどの読者に難しいか」は評価しない。jReadabilityは、日本語学習者向けの基準コーパスから文章難易度を6段階で示し、平均文長、動詞・助詞の比率などを使う。一方で、500〜1,000字程度の文章を前提とし、1文だけの測定や形態素解析誤りによるずれを適用限界として明記している。

自然度スコア、読解負荷、文章難易度を一つの数値で表すと、「人間らしいが専門家向けで難しい文章」と「平易だが機械的な文章」を区別できなくなる。

### 期待する動作

- 自然さ、局所的な読解負荷、対象読者に対する難易度を別の概念として出力する
- 文書長、ジャンル、想定読者が評価範囲外なら、数値を出さず理由を返す
- 難しい文章を自動的に悪い文章と判定しない

### 完了条件

- 初学者、一般読者、技術者向けの正解ラベル付き日本語コーパスを用意する
- 文長、語種、品詞比率、専門語密度の各特徴が判定へ与える影響を記録する
- 既存の`reading_load`と追加指標の相関、追加価値、誤判定を比較する
- 十分な校正データが得られない場合はスコアを実装せず、文書長などの観測値だけを出す

## P2: findingへ列・範囲を付け、CIとエディタで追跡しやすくする

### 観測結果

Suikoのfindingは行番号、カテゴリ、抜粋を返すが、列と対象範囲を返さない。同じ表現が一行に複数ある場合、機械側は対象を一意に選べない。textlintは複数formatterと修正情報、RedPenとValeはエディタやCIへ接続する出力経路を持つ。

### 期待する動作

- findingがUTF-8の日本語を含んでも、対象箇所を一意に指せる
- 人間向け出力、JSON、将来のGitHub注釈やエディタ連携が同じ位置情報を使う
- 既存JSON利用者への互換性を壊さない

### 完了条件

- byte offset、Unicode文字列、表示列のどれを公開契約にするか決め、before/after JSONを文書化する
- 単一行、複数行、同一表現の複数出現、全角文字、結合文字のfixtureを追加する
- 既存フィールドを維持したまま開始位置と終了位置を出力する
- GitHub Actions向けformatterまたはSARIFは、位置情報の安定後に別タスクとして評価する

## P2: 安全な修正候補とdiff表示を評価する

### 観測結果

textlintは修正可能なルールの`--fix --dry-run`、prhは`--diff`と明示的な置換を持つ。Suikoは意図した反復や著者の声を壊さないため、自動修正を実装していない。この境界は妥当だが、表記や定型的な冗長語のように、決定的な置換候補を安全に提示できるルールまで手作業になっている。

### 期待する動作

- 既定は読み取り専用の診断を維持する
- 機械的に安全と確認したルールだけ、変更前文字列を含む修正候補またはdiffを出せる
- 事実、引用、コード、URL、固有名詞、意図した反復を推測で書き換えない

### 完了条件

- 対象範囲の位置情報を先に実装する
- 修正可能カテゴリを明示的なallowlistにし、意味変更を伴うカテゴリを除外する
- 最初はファイルを書き換えず、JSON suggestionとdiff previewだけを提供する
- preimageが一致しない変更を適用しない回帰テストを追加する

## P2: Agent Skillの導入経路を再現可能にする

### 観測結果

`npx skills add <repository> --list`では、リポジトリ内の`suiko`を検出できる。Skillは構造検証にも通り、`suiko`から参照するファイルにも欠落はない。

一方、READMEは`cargo install suiko`と`$suiko`で利用できることを別々に示すだけで、Agent Skillを登録する手順を示していない。`cargo install`はCLIを導入するがSkillをエージェントへ登録せず、Skill管理CLIによる登録は`suiko`バイナリを導入しない。また、登録されたSkillだけを使う環境では、「このSkillのリポジトリで`cargo run`する」という現在の代替手段を使えない場合がある。

### 期待する動作

- CLIとAgent Skillが別の導入対象であることを、利用者が一つの手順から理解できる
- `$suiko`を導入した直後にCLIの有無を判定し、欠けている場合は実行可能な導入方法か手動診断へ進める
- Suikoと関係のないSkillを列挙せず、`suiko`だけがリポジトリ指定時の一覧に現れる

### 完了条件

- READMEにCLI導入とSkill登録を分けて記載し、`--skill suiko`を使う最小コマンドを示す
- 空の一時環境でSkillの一覧取得と登録を試し、`SKILL.md`、`agents/openai.yaml`、参照資料、assetが揃うことを確認する
- CLIがない環境で、存在しないリポジトリルートを前提にせず、導入案内または`manual-checklist.md`へ到達できることを確認する

## P2: CI・エディタ・Agentから同じ診断結果を利用できるようにする

### 観測結果

Suikoは標準入力とJSONを持つため外部プロセスから実行できるが、利用側がコマンド起動、JSON変換、ソース位置への対応付けを個別に実装する必要がある。textlintはformatterとMCP、Shodoは行・文字位置を返すAPI、RedPenとValeはエディタやCIとの連携経路を持つ。Harperは英語専用だが、Rust製の同種ツールをLSP、Wasm、複数エディタへ展開している。SuikoはREADMEでMCPを意図的な非対象としているが、標準入力とJSONだけで実際のAgent利用が十分かは測っていない。

### 期待する動作

- CLI、人間向け表示、GitHub上の注釈、Agentから見たfindingが同じカテゴリと位置を指す
- 連携層が診断ルールを再実装せず、Suiko本体を唯一の判定元として使う
- 常駐サーバーやネットワーク接続なしでも、現在のCLI利用を継続できる

### 設計上の確認事項

最初からMCPサーバーを実装しない。位置情報と安定したJSONを整えた後、次の順で利用時の手間と遅延を測る。

1. GitHub Actions向けの注釈formatter
2. エディタが読めるSARIFまたは既存診断形式
3. Agentから標準入力とJSONで呼ぶ薄いラッパー
4. 上記で不足が実証された場合だけMCPまたはLSP

### 完了条件

- 代表的なCI、エディタ、Agent利用を各1シナリオ用意し、必要な変換処理と実行時間を記録する
- 同一入力についてCLI JSONと連携先のカテゴリ、severity、位置が一致する回帰テストを追加する
- 新しい連携方式を追加しても、通常バイナリのオフライン実行と既存JSONを維持する
- MCPまたはLSPを採用しない場合も、標準入力とJSONで十分だった測定結果を残す

## P3: 一般校正との境界を比較fixtureで固定する

### 観測結果

Typoless、Just Right!、文賢、enno、LanguageToolは、誤字脱字、誤用、表記揺れ、用語基準、訂正候補など一般校正の範囲を広く扱う。SuikoはREADMEで一般校正の網羅を対象外としているが、どの問題を外部ツールへ任せ、どの問題を自然さの診断として扱うかを実行結果で固定していない。

境界が曖昧なままルールを追加すると、Suikoが小さな校正器の再実装になり、日本語の自然さに固有の検出を改善する時間が失われる。逆に、頻出する明白なタイポまで一切指ささない場合、単一バイナリだけを使う利用者の期待とずれる可能性がある。

### 期待する動作

- 自然さ、一般校正、組織固有ルールの担当範囲をfixtureと文書で説明できる
- Suikoで扱わない問題は、併用すべきツールまたは工程を案内できる
- 新しい一般校正ルールは、十分な頻度と高い適合率が確認できたものだけ採用する

### 完了条件

- 誤字脱字、誤用、表記揺れ、敬語、公用文、差別・リスク表現を含む比較fixtureを作る
- Suiko、利用可能なオープンソース校正器、規約上実行可能な商用サービスで結果を比較する
- 各カテゴリを「Suikoで実装」「既存ツールと併用」「人間判断へ残す」に分類する
- 実装対象は誤検知率と保守コストを記録し、競合の非公開辞書を推測で複製しない

## P3: ルール拡張と対応形式の優先順位を実利用から決める

### 観測結果

textlintはJavaScriptルールとprocessor、RedPenは複数マークアップと拡張、ValeはYAMLスタイルと複数形式を扱う。TypolessはWord、Excel、PowerPoint、PDF、ブラウザから利用できる。SuikoはMarkdownとプレーンテキスト、組み込みルール、`.suiko.toml`の無効化と理由付き許可に絞っている。

汎用プラグイン機構を追加すると配布と再現性の強みを薄める。一方、Re:VIEWやAsciiDoc、プロジェクト固有語彙の需要が実際にあるなら、現在の固定境界では不足する。

### 完了条件

- 実際の利用リポジトリから必要な形式とカスタムルール例を集める
- まず宣言的な語彙・禁止表現で満たせるか確認し、任意コード実行型プラグインは最後に検討する
- 新形式を追加する場合はfront matter、コード、リンク、表、コメントのマスクfixtureを形式ごとに追加する
- OfficeやPDFは本体で解析する前に、既存の抽出器から標準入力へ渡す方法で要求を満たせるか確認する
- 未導入の機能はREADMEの比較表で代替ツールを案内する

## 解消済み: 意味的な「話題の平板さ」を検出する価値を評価する

### 観測結果

表現と文長を変えながら同じ主張を反復する文書と、一つのAPI障害を観測から対策まで掘る文書を作成した。既存検出は前者へ1件、後者へ2件発火し、意味の進展自体は区別できなかった。

量子化済み多言語埋め込みの比較実装では、隣接文類似度の標準偏差が前者0.1505、後者0.1884だったが、平均値は近く個々の範囲も大きく重なった。2文書だけなら閾値を置けても、実文書の誤検知率を支える根拠にならない。

実行面ではモデルキャッシュ257 MiB、初回取得と初期化50.6秒、キャッシュ済み初期化653ミリ秒、短文書1件81〜138ミリ秒、ピーク常駐メモリ約740 MBだった。追加価値の根拠に対して負担が大きく、実行時ダウンロードなしの境界も壊すため、version 0.1には実装しない。評価詳細は`eval/semantic-study.md`、目視で確認する手順はREADMEとSkillへ記録した。

## 実装順序

依存関係を無視して機能を並行追加しない。次の順に進め、各段階でfixture、実コーパス、公開JSONの互換性を確認する。

1. `suiko-eval`の評価集合を増やし、現在の検出差を誤検知と見落としへ分類する
2. 一般校正との比較fixtureを作り、Suikoが受け持つカテゴリを固定する
3. `terms`の候補精度を再校正し、複数ファイルの用語監査契約を決める
4. findingの列・範囲を公開JSONへ追加する
5. 局所AIパターンをpositive/negative fixtureで評価し、有効なカテゴリだけ追加する
6. 読者別難易度が既存の読解負荷に追加価値を持つか評価する
7. 位置情報を使った安全なsuggestionとdiff previewを追加する
8. GitHub注釈、SARIF、Agentラッパーの順で連携コストを測る
9. Skillの発火評価と導入テストを自動化する
10. 対応形式、汎用拡張、MCP/LSP、意味的検出は、前段で不足が残ったものだけ採否を決める

各項目は「競合と同じ件数になった」では完了としない。正解ラベル付き評価で診断品質が改善し、通常バイナリのサイズ、起動時間、決定性を許容範囲に保てたことを完了条件とする。

## 解消を確認した項目

次の項目は、現行コードの回帰テストとCLIの最小再現で解消を確認したため、未完了TODOから移した。

- Rust 1.97の `collapsible_if` 5件。let chainへ整理し、全featureのClippyとテスト28件が成功
- `evaluation` featureのテストが既定CIから漏れていた問題。CIとREADMEを`--all-features --all-targets`へ統一
- `outline`の箇条書き合成表記を互換化し、29原稿の抽出項目がすべて一致
- `terms`が英小文字、1文字固有名詞、中黒で始まる語を大量に候補化する問題
- `terms`が初出行だけを見て、隣接行の説明手掛かりを落とす問題
- 名詞化の「の」を無生物主語として扱う`inanimate_subject_morph`の誤検出
- コードフェンス内の見出し風コメントを`terms`が抽出する問題
- 埋め込み引用行を`repeated_sentence_lead`などの本文統計へ含める問題
- インラインHTMLのタグ名と属性名を`terms`が抽出する問題

バッククォートとチルダのコードフェンス、通常のMarkdownリンク、HTML内の表示テキストを含む回帰テストがある。Rust 1.97.1では`fmt`、Clippy、全テスト、packageが成功する。
